# 后端待补接口 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按照设计文档 `docs/superpowers/specs/2026-04-13-后端待补接口-design.md` 实现 3 个新接口（用户帖子列表、用户收藏列表、图片上传 multipart）与数据库字段 `favorites_visibility`。

**Architecture:** go-zero 三层（handler → logic → model/rpc）。Gateway REST → gRPC 调 User/Content/Media 服务。本次 Interaction 服务仅改 proto 不实现；Media 服务 gateway 侧接入，但 server logic 不实现；`GetUserFavorites` 返回空列表 stub。

**Tech Stack:** go-zero v1.10、gRPC、MySQL + go-zero sqlx、goctl 1.10.1、standard go test + testify。

---

## 文件结构

| 路径 | 变更 | 责任 |
|---|---|---|
| `proto/content/content.proto` | 修改 | 新增 `GetPostsByIds` RPC、`GetUserPostsReq.sort_by` |
| `proto/user/user.proto` | 修改 | `UserInfo.favorites_visibility` |
| `proto/interaction/interaction.proto` | 修改 | 新增 `BatchCheckLiked`、`BatchCheckFavorited`（仅 proto） |
| `app/content/pb/...` | 重新生成 | Content RPC pb |
| `app/content/contentservice/content_service.go` | 重新生成 | Content RPC client |
| `app/content/internal/server/...` | 重新生成 | Content RPC server 骨架 |
| `app/content/internal/model/post_model.go` | 修改 | `FindByAuthorId` 加 `sortBy` |
| `app/content/internal/logic/get_user_posts_logic.go` | 修改 | 支持 sortBy |
| `app/content/internal/logic/get_posts_by_ids_logic.go` | 新增 | 批量按 ID 查帖子 |
| `app/user/pb/...` | 重新生成 | User RPC pb |
| `app/user/internal/logic/convert.go` | 修改 | 映射 `FavoritesVisibility` |
| `app/user/internal/logic/get_user_logic.go` | 不变 | 复用现有逻辑 |
| `app/user/internal/model/user_profile_model_gen.go` | 重新生成 | 新增 `FavoritesVisibility` 字段 |
| `deploy/sql/xbh_user.sql` | 修改 | 新增列 |
| `app/media/...` | 新增（scaffold） | Media RPC pb + client（server logic 空壳） |
| `app/gateway/gateway.api` | 修改 | 类型、路由变更 |
| `app/gateway/internal/handler/*` | 重新生成+手改 | routes + 手写 upload handler |
| `app/gateway/internal/svc/service_context.go` | 修改 | 注入 Media client |
| `app/gateway/internal/config/config.go` | 修改 | 新增 `MediaRpc` |
| `app/gateway/etc/gateway.yaml` | 修改 | 新增 MediaRpc 配置 |
| `app/gateway/internal/logic/user/get_user_logic.go` | 修改 | 移除权限限制，加 FavoritesVisible |
| `app/gateway/internal/logic/user/get_user_posts_logic.go` | 新增 | 用户帖子列表 |
| `app/gateway/internal/logic/user/get_user_favorites_logic.go` | 新增 | 用户收藏列表（含权限 + stub） |
| `app/gateway/internal/logic/image/upload_image_logic.go` | 重写 | multipart file → streaming Media RPC |
| `app/gateway/internal/handler/image/upload_image_handler.go` | 手写 | multipart 解析 |
| `pkg/errx/codes.go` | 修改 | 新增 `FavoritesPrivate` 错误码 |

---

## Task 1：proto/content.proto 新增 GetPostsByIds + sort_by

**Files:**
- Modify: `proto/content/content.proto`

- [ ] **Step 1：在 ContentService 里新增 RPC 定义**

在 `service ContentService { ... }` 的 `GetUserPosts` 下方新增一行：

```protobuf
// 批量按 ID 获取帖子（用于收藏列表聚合）
rpc GetPostsByIds(GetPostsByIdsReq) returns (GetPostsByIdsResp);
```

- [ ] **Step 2：修改 `GetUserPostsReq` 增加 sort_by 字段**

将：
```protobuf
message GetUserPostsReq {
  int64 user_id   = 1;
  int32 page      = 2;
  int32 page_size = 3;
}
```
改为：
```protobuf
message GetUserPostsReq {
  int64 user_id   = 1;
  int32 page      = 2;
  int32 page_size = 3;
  int32 sort_by   = 4; // 1=最新 2=热门
}
```

- [ ] **Step 3：在文件末尾新增 GetPostsByIds 消息**

```protobuf
// 批量按 ID 获取帖子请求
message GetPostsByIdsReq {
  repeated int64 post_ids = 1;
}

// 批量按 ID 获取帖子响应
message GetPostsByIdsResp {
  repeated PostInfo posts = 1;
}
```

- [ ] **Step 4：重新生成 Content RPC 代码**

Run (Windows bash):
```bash
cd "D:/Learning/projects/work projects/little-white-box" && \
goctl rpc protoc proto/content/content.proto \
  --go_out=app/content/pb \
  --go-grpc_out=app/content/pb \
  --zrpc_out=app/content \
  --style=go_zero
```

Expected: 在 `app/content/pb/xiaobaihe/content/pb/` 下更新 pb 文件；`app/content/contentservice/content_service.go` 含 `GetPostsByIds` 方法；`app/content/internal/server/` 生成 `getpostsbyidslogic` server handler。

- [ ] **Step 5：验证编译**

```bash
cd app/content && go build ./... 2>&1 | head -20
```
Expected: 只报 `GetPostsByIdsLogic` 未实现 的错误（因为 server handler 调用了不存在的 logic）—— 后续任务解决。其他编译通过。

- [ ] **Step 6：提交**

```bash
cd "D:/Learning/projects/work projects/little-white-box"
git add proto/content/content.proto app/content/pb app/content/contentservice app/content/internal/server
git commit -m "feat(content): 新增 GetPostsByIds RPC 与 GetUserPostsReq.sort_by"
```

---

## Task 2：proto/user.proto 新增 favorites_visibility

**Files:**
- Modify: `proto/user/user.proto`

- [ ] **Step 1：UserInfo 新增字段**

找到 `message UserInfo { ... }`，在 `created_at` 之后新增：

```protobuf
message UserInfo {
  int64  id              = 1;
  string username        = 2;
  string nickname        = 3;
  string avatar_url      = 4;
  string bio             = 5;
  int32  level           = 6;
  int64  follower_count  = 7;
  int64  following_count = 8;
  int64  post_count      = 9;
  int64  like_count      = 10;
  int64  created_at      = 11;
  int32  favorites_visibility = 12; // 1=公开 2=私密
}
```

- [ ] **Step 2：重新生成 User RPC 代码**

```bash
cd "D:/Learning/projects/work projects/little-white-box" && \
goctl rpc protoc proto/user/user.proto \
  --go_out=app/user/pb \
  --go-grpc_out=app/user/pb \
  --zrpc_out=app/user \
  --style=go_zero
```

- [ ] **Step 3：验证 pb 文件含新字段**

```bash
grep "FavoritesVisibility" app/user/pb/xiaobaihe/user/pb/user.pb.go | head -5
```
Expected: 输出包含 `FavoritesVisibility int32`。

- [ ] **Step 4：提交**

```bash
git add proto/user/user.proto app/user/pb app/user/userservice app/user/internal/server
git commit -m "feat(user): UserInfo 新增 favorites_visibility 字段"
```

---

## Task 3：proto/interaction.proto 新增批量查询 RPC（仅 proto）

**Files:**
- Modify: `proto/interaction/interaction.proto`

- [ ] **Step 1：新增 RPC 定义**

在 `service InteractionService {}` 中 `GetCounts` 上方新增两行：

```protobuf
// 批量检查点赞
rpc BatchCheckLiked(BatchCheckLikedReq) returns (BatchCheckLikedResp);
// 批量检查收藏
rpc BatchCheckFavorited(BatchCheckFavoritedReq) returns (BatchCheckFavoritedResp);
```

- [ ] **Step 2：在文件末尾新增消息定义**

```protobuf
// 批量检查点赞请求
message BatchCheckLikedReq {
  int64          user_id     = 1;
  repeated int64 target_ids  = 2;
  int32          target_type = 3;
}

// 批量检查点赞响应
message BatchCheckLikedResp {
  map<int64, bool> liked_map = 1;
}

// 批量检查收藏请求
message BatchCheckFavoritedReq {
  int64          user_id  = 1;
  repeated int64 post_ids = 2;
}

// 批量检查收藏响应
message BatchCheckFavoritedResp {
  map<int64, bool> favorited_map = 1;
}
```

- [ ] **Step 3：不生成代码，仅提交 proto 变更**

（用户指示：Interaction 服务本次仅改 proto，不生成 Go 代码）

```bash
git add proto/interaction/interaction.proto
git commit -m "feat(interaction): 新增 BatchCheckLiked/BatchCheckFavorited RPC 定义（仅 proto）"
```

---

## Task 4：xbh_user.sql 新增 favorites_visibility 列

**Files:**
- Modify: `deploy/sql/xbh_user.sql`

- [ ] **Step 1：修改 user_profile 表定义**

在 `user_profile` 表定义中 `status` 字段之后（`created_at` 之前）新增一行：

```sql
    `status` TINYINT DEFAULT 1 COMMENT '状态 0:禁用 1:正常',
    `favorites_visibility` TINYINT NOT NULL DEFAULT 1 COMMENT '收藏可见性 1:公开 2:私密',
    `created_at` TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
```

- [ ] **Step 2：手动执行 migration（你已启动的 MySQL）**

```sql
USE `xbh_user`;
ALTER TABLE `user_profile`
  ADD COLUMN `favorites_visibility` TINYINT NOT NULL DEFAULT 1
  COMMENT '收藏可见性 1:公开 2:私密' AFTER `status`;
```

- [ ] **Step 3：验证列存在**

```sql
SHOW COLUMNS FROM `user_profile` LIKE 'favorites_visibility';
```
Expected: 返回一行，`Type=tinyint`，`Default=1`。

- [ ] **Step 4：提交**

```bash
git add deploy/sql/xbh_user.sql
git commit -m "feat(user): user_profile 新增 favorites_visibility 字段"
```

---

## Task 5：重新生成 user_profile model

**Files:**
- Regenerate: `app/user/internal/model/user_profile_model_gen.go`

- [ ] **Step 1：使用 goctl 重新生成 model**

```bash
cd "D:/Learning/projects/work projects/little-white-box/app/user/internal/model" && \
goctl model mysql ddl \
  --src="../../../../deploy/sql/xbh_user.sql" \
  --dir=. \
  --cache \
  --style=go_zero
```

（若 goctl 不接受多表 DDL，改用 datasource 方式连接本地 DB 重新生成：）

```bash
goctl model mysql datasource \
  --url="root:123456@tcp(127.0.0.1:3306)/xbh_user" \
  --table="user_profile" \
  --dir=. \
  --cache \
  --style=go_zero
```

- [ ] **Step 2：确认 UserProfile struct 包含新字段**

```bash
grep "FavoritesVisibility" app/user/internal/model/user_profile_model_gen.go
```
Expected: 输出 `FavoritesVisibility int64 \`db:"favorites_visibility"\``。

- [ ] **Step 3：验证编译**

```bash
cd app/user && go build ./... 2>&1 | head
```
Expected: 通过。

- [ ] **Step 4：提交**

```bash
git add app/user/internal/model
git commit -m "chore(user): 重新生成 user_profile model（含 favorites_visibility）"
```

---

## Task 6：User 服务 convert.go 映射 FavoritesVisibility

**Files:**
- Modify: `app/user/internal/logic/convert.go`

- [ ] **Step 1：修改 UserProfileToUserInfo 增加 FavoritesVisibility**

完整替换文件内容为：

```go
// storage conversion functions

package logic

import (
	"user/internal/model"
	"user/pb/xiaobaihe/user/pb"
)

func UserProfileToUserInfo(profile *model.UserProfile) *pb.UserInfo {
	return &pb.UserInfo{
		Id:                   profile.Id,
		Username:             profile.Username,
		Nickname:             profile.Nickname.String,
		AvatarUrl:            profile.AvatarUrl.String,
		Bio:                  profile.Bio.String,
		Level:                int32(profile.Level),
		FollowerCount:        profile.FollowerCount,
		FollowingCount:       profile.FollowingCount,
		PostCount:            profile.PostCount,
		LikeCount:            profile.LikeCount,
		CreatedAt:            profile.CreatedAt.Unix(),
		FavoritesVisibility:  int32(profile.FavoritesVisibility),
	}
}
```

- [ ] **Step 2：验证编译**

```bash
cd app/user && go build ./... 2>&1 | head
```
Expected: 通过。

- [ ] **Step 3：提交**

```bash
git add app/user/internal/logic/convert.go
git commit -m "feat(user): convert 映射 FavoritesVisibility"
```

---

## Task 7：Content PostModel.FindByAuthorId 增加 sortBy 支持

**Files:**
- Modify: `app/content/internal/model/post_model.go`
- Modify: `app/content/internal/logic/mock_models_test.go`

- [ ] **Step 1：修改 PostModel 接口签名**

在 `app/content/internal/model/post_model.go` 中把接口里的 `FindByAuthorId` 一行改为：

```go
FindByAuthorId(ctx context.Context, authorId int64, page, pageSize, sortBy int) ([]*Post, int64, error)
```

- [ ] **Step 2：修改实现**

将 `FindByAuthorId` 实现完整替换为：

```go
func (m *customPostModel) FindByAuthorId(ctx context.Context, authorId int64, page, pageSize, sortBy int) ([]*Post, int64, error) {
	offset := (page - 1) * pageSize

	orderBy := "`created_at` desc"
	if sortBy == 2 {
		orderBy = "`like_count` desc, `created_at` desc"
	}

	var posts []*Post
	query := fmt.Sprintf("select %s from %s where `author_id` = ? and `status` = 1 order by %s limit ?,?", postRows, m.table, orderBy)
	err := m.CachedConn.QueryRowsNoCacheCtx(ctx, &posts, query, authorId, offset, pageSize)
	if err != nil {
		return nil, 0, err
	}

	var total int64
	countQuery := fmt.Sprintf("select count(*) from %s where `author_id` = ? and `status` = 1", m.table)
	err = m.CachedConn.QueryRowNoCacheCtx(ctx, &total, countQuery, authorId)
	if err != nil {
		return nil, 0, err
	}

	return posts, total, nil
}
```

- [ ] **Step 3：同步更新 mock**

在 `app/content/internal/logic/mock_models_test.go` 中替换 `MockPostModel.FindByAuthorId`：

```go
func (m *MockPostModel) FindByAuthorId(ctx context.Context, authorId int64, page, pageSize, sortBy int) ([]*model.Post, int64, error) {
	args := m.Called(ctx, authorId, page, pageSize, sortBy)
	return args.Get(0).([]*model.Post), args.Get(1).(int64), args.Error(2)
}
```

- [ ] **Step 4：临时保留老调用点可编译（调用方在 Task 8 修改）**

此时 `get_user_posts_logic.go` 还调用旧签名，会编译错误 — 这是预期，下一任务修。

- [ ] **Step 5：提交**

```bash
git add app/content/internal/model/post_model.go app/content/internal/logic/mock_models_test.go
git commit -m "refactor(content): PostModel.FindByAuthorId 增加 sortBy 参数"
```

---

## Task 8：Content GetUserPosts logic 支持 sortBy

**Files:**
- Modify: `app/content/internal/logic/get_user_posts_logic.go`

- [ ] **Step 1：写失败测试（新增 sortBy 测试用例）**

在 `app/content/internal/logic/post_logic_test.go` 文件末尾新增：

```go
// ─── GetUserPosts ─────────────────────────────────────────────────────────────

func TestGetUserPostsLogic_SortBy(t *testing.T) {
	tests := []struct {
		name   string
		req    *pb.GetUserPostsReq
		sortBy int
	}{
		{"默认最新排序", &pb.GetUserPostsReq{UserId: 100, Page: 1, PageSize: 10, SortBy: 0}, 1},
		{"sortBy=1 最新", &pb.GetUserPostsReq{UserId: 100, Page: 1, PageSize: 10, SortBy: 1}, 1},
		{"sortBy=2 热门", &pb.GetUserPostsReq{UserId: 100, Page: 1, PageSize: 10, SortBy: 2}, 2},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			pm := new(MockPostModel)
			ptm := new(MockPostTagModel)
			pm.On("FindByAuthorId", mock.Anything, int64(100), 1, 10, tt.sortBy).
				Return([]*model.Post{}, int64(0), nil)
			svcCtx := &svc.ServiceContext{PostModel: pm, PostTagModel: ptm}
			l := NewGetUserPostsLogic(context.Background(), svcCtx)
			_, err := l.GetUserPosts(tt.req)
			require.NoError(t, err)
			pm.AssertExpectations(t)
		})
	}
}
```

- [ ] **Step 2：运行测试验证 FAIL**

```bash
cd app/content && go test ./internal/logic/ -run TestGetUserPostsLogic_SortBy -race 2>&1 | tail
```
Expected: FAIL — 因为当前 `GetUserPosts` 没传 sortBy。

- [ ] **Step 3：修改 logic 传 sortBy**

完整替换 `app/content/internal/logic/get_user_posts_logic.go` 中 `GetUserPosts` 方法（只修改 FindByAuthorId 调用行）：

```go
func (l *GetUserPostsLogic) GetUserPosts(in *pb.GetUserPostsReq) (*pb.GetUserPostsResp, error) {
	page := int(in.Page)
	pageSize := int(in.PageSize)
	if page <= 0 {
		page = 1
	}
	if pageSize <= 0 || pageSize > 50 {
		pageSize = 20
	}
	sortBy := int(in.SortBy)
	if sortBy != 2 {
		sortBy = 1
	}

	posts, total, err := l.svcCtx.PostModel.FindByAuthorId(l.ctx, in.UserId, page, pageSize, sortBy)
	if err != nil {
		return nil, fmt.Errorf("查询用户帖子失败: %w", err)
	}

	if len(posts) == 0 {
		return &pb.GetUserPostsResp{Posts: []*pb.PostInfo{}, Total: total}, nil
	}

	postIds := make([]int64, 0, len(posts))
	for _, post := range posts {
		postIds = append(postIds, post.Id)
	}
	tagsMap, err := l.svcCtx.PostTagModel.FindTagNamesByPostIds(l.ctx, postIds)
	if err != nil {
		l.Logger.Errorf("批量查询标签失败 err=%v", err)
		tagsMap = map[int64][]string{}
	}
	postInfos := make([]*pb.PostInfo, 0, len(posts))
	for _, post := range posts {
		postInfos = append(postInfos, PostToPostInfo(post, tagsMap[post.Id]))
	}

	return &pb.GetUserPostsResp{
		Posts: postInfos,
		Total: total,
	}, nil
}
```

- [ ] **Step 4：运行测试验证 PASS**

```bash
cd app/content && go test ./internal/logic/ -run TestGetUserPostsLogic_SortBy -race 2>&1 | tail
```
Expected: PASS。

- [ ] **Step 5：提交**

```bash
git add app/content/internal/logic/get_user_posts_logic.go app/content/internal/logic/post_logic_test.go
git commit -m "feat(content): GetUserPosts 支持 sortBy 1=最新 2=热门"
```

---

## Task 9：Content GetPostsByIdsLogic 实现

**Files:**
- Create: `app/content/internal/logic/get_posts_by_ids_logic.go`
- Modify: `app/content/internal/logic/post_logic_test.go`

- [ ] **Step 1：写失败测试**

在 `post_logic_test.go` 末尾新增：

```go
// ─── GetPostsByIds ────────────────────────────────────────────────────────────

func TestGetPostsByIdsLogic(t *testing.T) {
	tests := []struct {
		name      string
		req       *pb.GetPostsByIdsReq
		setupMock func(*MockPostModel, *MockPostTagModel)
		wantLen   int
	}{
		{
			name: "空 ID 列表返回空结果",
			req:  &pb.GetPostsByIdsReq{PostIds: []int64{}},
			setupMock: func(pm *MockPostModel, _ *MockPostTagModel) {
				pm.On("FindByIds", mock.Anything, mock.Anything).Return([]*model.Post{}, nil).Maybe()
			},
			wantLen: 0,
		},
		{
			name: "过滤已删除帖子（status != 1）",
			req:  &pb.GetPostsByIdsReq{PostIds: []int64{1, 2, 3}},
			setupMock: func(pm *MockPostModel, ptm *MockPostTagModel) {
				pm.On("FindByIds", mock.Anything, []int64{1, 2, 3}).Return([]*model.Post{
					{Id: 1, AuthorId: 100, Title: "t1", Status: 1},
					{Id: 2, AuthorId: 100, Title: "t2", Status: 2}, // 已删除
					{Id: 3, AuthorId: 100, Title: "t3", Status: 1},
				}, nil)
				ptm.On("FindTagNamesByPostIds", mock.Anything, mock.Anything).Return(map[int64][]string{}, nil)
			},
			wantLen: 2,
		},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			pm := new(MockPostModel)
			ptm := new(MockPostTagModel)
			if tt.setupMock != nil {
				tt.setupMock(pm, ptm)
			}
			svcCtx := &svc.ServiceContext{PostModel: pm, PostTagModel: ptm}
			l := NewGetPostsByIdsLogic(context.Background(), svcCtx)
			resp, err := l.GetPostsByIds(tt.req)
			require.NoError(t, err)
			assert.Len(t, resp.Posts, tt.wantLen)
		})
	}
}
```

- [ ] **Step 2：运行测试验证 FAIL**

```bash
cd app/content && go test ./internal/logic/ -run TestGetPostsByIdsLogic -race 2>&1 | tail
```
Expected: FAIL — `NewGetPostsByIdsLogic undefined`。

- [ ] **Step 3：实现 logic**

创建 `app/content/internal/logic/get_posts_by_ids_logic.go`：

```go
package logic

import (
	"context"
	"esx/app/content/pb/xiaobaihe/content/pb"
	"fmt"

	"esx/app/content/internal/svc"

	"github.com/zeromicro/go-zero/core/logx"
)

type GetPostsByIdsLogic struct {
	ctx    context.Context
	svcCtx *svc.ServiceContext
	logx.Logger
}

func NewGetPostsByIdsLogic(ctx context.Context, svcCtx *svc.ServiceContext) *GetPostsByIdsLogic {
	return &GetPostsByIdsLogic{
		ctx:    ctx,
		svcCtx: svcCtx,
		Logger: logx.WithContext(ctx),
	}
}

// GetPostsByIds 批量按 ID 查询帖子（过滤软删除/未发布）
func (l *GetPostsByIdsLogic) GetPostsByIds(in *pb.GetPostsByIdsReq) (*pb.GetPostsByIdsResp, error) {
	if len(in.PostIds) == 0 {
		return &pb.GetPostsByIdsResp{Posts: []*pb.PostInfo{}}, nil
	}

	posts, err := l.svcCtx.PostModel.FindByIds(l.ctx, in.PostIds)
	if err != nil {
		return nil, fmt.Errorf("批量查询帖子失败: %w", err)
	}

	// 过滤 status != 1（软删除或草稿）
	filtered := make([]*pb.PostInfo, 0, len(posts))
	validIds := make([]int64, 0, len(posts))
	validPosts := make([]*model.Post, 0, len(posts)) //nolint
	for _, p := range posts {
		if p.Status != 1 {
			continue
		}
		validIds = append(validIds, p.Id)
	}
	_ = validPosts // unused when we don't need it

	tagsMap, err := l.svcCtx.PostTagModel.FindTagNamesByPostIds(l.ctx, validIds)
	if err != nil {
		l.Logger.Errorf("批量查询标签失败 err=%v", err)
		tagsMap = map[int64][]string{}
	}
	for _, p := range posts {
		if p.Status != 1 {
			continue
		}
		filtered = append(filtered, PostToPostInfo(p, tagsMap[p.Id]))
	}

	return &pb.GetPostsByIdsResp{Posts: filtered}, nil
}
```

然后清理：删除 `validPosts` 未使用行和 `"esx/app/content/internal/model"` 不需要的导入（上面示例已是最终形态，但要确保 import 中只有需要的）。最终 import 简化为：

```go
import (
	"context"
	"esx/app/content/pb/xiaobaihe/content/pb"
	"fmt"

	"esx/app/content/internal/svc"

	"github.com/zeromicro/go-zero/core/logx"
)
```

并在函数里删除 `validPosts` 相关 2 行。最终 function 体：

```go
func (l *GetPostsByIdsLogic) GetPostsByIds(in *pb.GetPostsByIdsReq) (*pb.GetPostsByIdsResp, error) {
	if len(in.PostIds) == 0 {
		return &pb.GetPostsByIdsResp{Posts: []*pb.PostInfo{}}, nil
	}

	posts, err := l.svcCtx.PostModel.FindByIds(l.ctx, in.PostIds)
	if err != nil {
		return nil, fmt.Errorf("批量查询帖子失败: %w", err)
	}

	validIds := make([]int64, 0, len(posts))
	for _, p := range posts {
		if p.Status == 1 {
			validIds = append(validIds, p.Id)
		}
	}

	tagsMap, err := l.svcCtx.PostTagModel.FindTagNamesByPostIds(l.ctx, validIds)
	if err != nil {
		l.Logger.Errorf("批量查询标签失败 err=%v", err)
		tagsMap = map[int64][]string{}
	}

	result := make([]*pb.PostInfo, 0, len(validIds))
	for _, p := range posts {
		if p.Status != 1 {
			continue
		}
		result = append(result, PostToPostInfo(p, tagsMap[p.Id]))
	}
	return &pb.GetPostsByIdsResp{Posts: result}, nil
}
```

- [ ] **Step 4：接入 RPC server**

打开 `app/content/internal/server/contentserviceserver.go`（goctl 已生成），确认其 `GetPostsByIds` 方法已调用 `logic.NewGetPostsByIdsLogic(...)` — 如果是 goctl 自动生成的，它会自动产出正确骨架，无需手改。否则在对应文件中补：

```go
func (s *ContentServiceServer) GetPostsByIds(ctx context.Context, in *pb.GetPostsByIdsReq) (*pb.GetPostsByIdsResp, error) {
	l := logic.NewGetPostsByIdsLogic(ctx, s.svcCtx)
	return l.GetPostsByIds(in)
}
```

- [ ] **Step 5：运行测试验证 PASS**

```bash
cd app/content && go test ./internal/logic/ -run TestGetPostsByIdsLogic -race 2>&1 | tail
```
Expected: PASS。

- [ ] **Step 6：跑全量测试确保无回归**

```bash
cd app/content && go test ./... -race 2>&1 | tail -20
```
Expected: 全部通过。

- [ ] **Step 7：提交**

```bash
git add app/content/internal/logic/get_posts_by_ids_logic.go app/content/internal/logic/post_logic_test.go app/content/internal/server
git commit -m "feat(content): 实现 GetPostsByIds RPC"
```

---

## Task 10：生成 Media RPC 代码（scaffold，server logic 暂空）

**Files:**
- Create: `app/media/**/*` (goctl 生成)

- [ ] **Step 1：生成 Media RPC 目录骨架**

```bash
cd "D:/Learning/projects/work projects/little-white-box" && \
goctl rpc protoc proto/media/media.proto \
  --go_out=app/media/pb \
  --go-grpc_out=app/media/pb \
  --zrpc_out=app/media \
  --style=go_zero
```

Expected: 生成 `app/media/media.go`（入口）、`app/media/pb/...`、`app/media/mediaservice/...`、`app/media/internal/...`（logic 为 TODO 骨架）。

- [ ] **Step 2：确认生成了 client 代码**

```bash
ls app/media/mediaservice/
grep "UploadImage" app/media/mediaservice/*.go | head
```
Expected: `media_service.go` 存在，`UploadImage` 方法签名含 streaming。

- [ ] **Step 3：验证整个仓库编译**

```bash
cd "D:/Learning/projects/work projects/little-white-box" && \
go build ./... 2>&1 | tail -20
```

Expected: Media 内部 logic 可能报 TODO 占位的 return 问题（如 goctl 生成的 nil, nil），但 `go build` 一般通过。如果 Media 服务的 logic 文件编译失败，把这些 logic 文件的 todo 改为简单返回 `nil, fmt.Errorf("not implemented")`（保持编译通过）。

- [ ] **Step 4：提交**

```bash
git add app/media
git commit -m "chore(media): 生成 Media RPC 代码骨架（server logic 未实现）"
```

---

## Task 11：pkg/errx 新增 FavoritesPrivate 错误码

**Files:**
- Modify: `pkg/errx/codes.go`

- [ ] **Step 1：新增错误码与消息**

在 `pkg/errx/codes.go` 中：

在 "互动相关错误码" 区（3001-3006 之后）新增：

```go
	CannotFollowSelf    = 3006
	FavoritesPrivate    = 3007
```

在 `codeMsg` map 里 `CannotFollowSelf` 后添加：

```go
	CannotFollowSelf:    "不能关注自己",
	FavoritesPrivate:    "收藏列表已设为私密",
```

- [ ] **Step 2：验证编译**

```bash
cd pkg/errx && go build ./... 2>&1 | tail
```
Expected: 通过。

- [ ] **Step 3：提交**

```bash
git add pkg/errx/codes.go
git commit -m "feat(errx): 新增 FavoritesPrivate 错误码"
```

---

## Task 12：gateway.api 类型与路由变更

**Files:**
- Modify: `app/gateway/gateway.api`

- [ ] **Step 1：修改 PostItem 新增字段**

找到 `type PostItem { ... }`，在 `CreatedAt` 之前新增 3 个字段：

```go
type PostItem {
    Id            int64    `json:"id"`
    AuthorId      int64    `json:"authorId"`
    AuthorName    string   `json:"authorName"`
    AuthorAvatar  string   `json:"authorAvatar"`
    Title         string   `json:"title"`
    Content       string   `json:"content"`
    Images        []string `json:"images"`
    Tags          []string `json:"tags"`
    ViewCount     int64    `json:"viewCount"`
    LikeCount     int64    `json:"likeCount"`
    CommentCount  int64    `json:"commentCount"`
    FavoriteCount int64    `json:"favoriteCount"`
    IsLiked       bool     `json:"isLiked"`
    IsFavorited   bool     `json:"isFavorited"`
    CreatedAt     int64    `json:"createdAt"`
}
```

- [ ] **Step 2：修改 GetUserResp 新增 FavoritesVisible**

```go
type GetUserResp {
    Id               int64  `json:"id"`
    Username         string `json:"username"`
    Nickname         string `json:"nickname"`
    AvatarUrl        string `json:"avatarUrl"`
    Bio              string `json:"bio"`
    Level            int32  `json:"level"`
    FollowerCount    int64  `json:"followerCount"`
    FollowingCount   int64  `json:"followingCount"`
    PostCount        int64  `json:"postCount"`
    FavoritesVisible bool   `json:"favoritesVisible"`
}
```

- [ ] **Step 3：新增用户帖子/收藏接口类型**

在 `// 用户相关类型` 区段末尾（在 `UpdateProfileResp` 之后、`// 帖子相关类型` 之前）新增：

```go
type (
    GetUserPostsReq {
        UserId   int64 `path:"userId"`
        Page     int32 `form:"page,default=1"`
        PageSize int32 `form:"pageSize,default=20"`
        SortBy   int32 `form:"sortBy,optional,default=1"`
    }
)

type (
    GetUserFavoritesReq {
        UserId   int64 `path:"userId"`
        Page     int32 `form:"page,default=1"`
        PageSize int32 `form:"pageSize,default=20"`
    }
)
```

- [ ] **Step 4：修改 UploadImageReq 为空**

```go
type (
    UploadImageReq {}
    UploadImageResp {
        MediaId      int64  `json:"mediaId"`
        Url          string `json:"url"`
        ThumbnailUrl string `json:"thumbnailUrl"`
    }
)
```

- [ ] **Step 5：路由变更——拆分 user group**

找到现有 user group，整个块替换为 **两个** server group：

```go
@server (
    prefix: /api/v1
    group:  user
)
service gateway {
    @doc "获取用户信息"
    @handler GetUser
    get /user/:userId (GetUserReq) returns (GetUserResp)

    @doc "获取用户发布的帖子列表"
    @handler GetUserPosts
    get /users/:userId/posts (GetUserPostsReq) returns (GetPostListResp)

    @doc "获取用户的收藏帖子列表"
    @handler GetUserFavorites
    get /users/:userId/favorites (GetUserFavoritesReq) returns (GetPostListResp)
}

@server (
    prefix: /api/v1
    jwt:    Auth
    group:  user
)
service gateway {
    @doc "更新用户资料"
    @handler UpdateProfile
    put /user/profile (UpdateProfileReq) returns (UpdateProfileResp)

    @doc "关注用户"
    @handler Follow
    post /user/follow (FollowReq) returns (FollowResp)

    @doc "取消关注"
    @handler Unfollow
    delete /user/follow (UnfollowReq) returns (UnfollowResp)
}
```

- [ ] **Step 6：重新生成 gateway handler/types/routes**

```bash
cd "D:/Learning/projects/work projects/little-white-box/app/gateway" && \
goctl api go -api gateway.api -dir . --style=go_zero
```

Expected: 生成新的 `get_user_posts_handler.go`、`get_user_favorites_handler.go`、`get_user_handler.go` 移到公开 group；`types/types.go` 更新；`routes.go` 更新。

**注意**：`upload_image_handler.go` 可能被重写成标准 httpx.Parse 版本 — 后续 Task 18 会覆盖。

- [ ] **Step 7：先备份将被覆盖的 upload handler（如需）**

（此步骤通常不需要，因为 goctl 不重写 logic 文件，仅生成 handler 骨架；但若 Step 6 改写了 logic 文件把你已有的覆盖了，从 git 恢复）：

```bash
git checkout -- app/gateway/internal/logic/image/upload_image_logic.go 2>/dev/null || true
```

- [ ] **Step 8：验证编译（允许新 logic 占位报错）**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: `get_user_posts_logic.go`、`get_user_favorites_logic.go` 是 goctl 生成的空骨架（return nil）— 编译通过。

- [ ] **Step 9：提交**

```bash
git add app/gateway/gateway.api app/gateway/internal
git commit -m "feat(gateway): 新增 GetUserPosts/GetUserFavorites 路由，改公开 GetUser"
```

---

## Task 13：Gateway Config & ServiceContext 注入 Media client

**Files:**
- Modify: `app/gateway/internal/config/config.go`
- Modify: `app/gateway/internal/svc/service_context.go`
- Modify: `app/gateway/etc/gateway.yaml`

- [ ] **Step 1：Config 新增 MediaRpc**

完整替换 `app/gateway/internal/config/config.go`：

```go
// Code scaffolded by goctl. Safe to edit.
// goctl 1.10.1

package config

import (
	"github.com/zeromicro/go-zero/rest"
	"github.com/zeromicro/go-zero/zrpc"
)

type Config struct {
	RestConf rest.RestConf
	Auth     struct {
		AccessSecret string
		AccessExpire int64
	}
	UserRpc    zrpc.RpcClientConf
	ContentRpc zrpc.RpcClientConf
	MediaRpc   zrpc.RpcClientConf
}
```

- [ ] **Step 2：ServiceContext 注入 MediaService**

完整替换 `app/gateway/internal/svc/service_context.go`：

```go
// Code scaffolded by goctl. Safe to edit.
// goctl 1.10.1

package svc

import (
	"esx/app/content/contentservice"
	"esx/app/media/mediaservice"
	"gateway/internal/config"
	"user/userservice"

	"github.com/zeromicro/go-zero/zrpc"
)

type ServiceContext struct {
	Config         config.Config
	UserService    userservice.UserService
	ContentService contentservice.ContentService
	MediaService   mediaservice.MediaService
}

func NewServiceContext(c config.Config) *ServiceContext {
	userClient := zrpc.MustNewClient(c.UserRpc)
	contentClient := zrpc.MustNewClient(c.ContentRpc)
	mediaClient := zrpc.MustNewClient(c.MediaRpc)

	return &ServiceContext{
		Config:         c,
		UserService:    userservice.NewUserService(userClient),
		ContentService: contentservice.NewContentService(contentClient),
		MediaService:   mediaservice.NewMediaService(mediaClient),
	}
}
```

**注意**：确认 `mediaservice` 的 import 路径与 Task 10 生成结果一致；如果 go.mod 里 media 模块的 module 名不是 `esx/app/media/mediaservice`，按实际路径调整。

- [ ] **Step 3：etc/gateway.yaml 新增 MediaRpc 配置**

在 `app/gateway/etc/gateway.yaml` 末尾新增：

```yaml
MediaRpc:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: media
```

- [ ] **Step 4：验证编译**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: 通过。

- [ ] **Step 5：提交**

```bash
git add app/gateway/internal/config/config.go app/gateway/internal/svc/service_context.go app/gateway/etc/gateway.yaml
git commit -m "feat(gateway): 注入 MediaService client"
```

---

## Task 14：Gateway GetUser logic 改造（公开访问 + FavoritesVisible）

**Files:**
- Modify: `app/gateway/internal/logic/user/get_user_logic.go`

- [ ] **Step 1：完整替换 logic**

```go
// Code scaffolded by goctl. Safe to edit.
// goctl 1.10.1

package user

import (
	"context"
	"fmt"
	"user/pb/xiaobaihe/user/pb"

	"gateway/internal/svc"
	"gateway/internal/types"

	"github.com/zeromicro/go-zero/core/logx"
)

type GetUserLogic struct {
	logx.Logger
	ctx    context.Context
	svcCtx *svc.ServiceContext
}

// 获取用户信息（公开接口）
func NewGetUserLogic(ctx context.Context, svcCtx *svc.ServiceContext) *GetUserLogic {
	return &GetUserLogic{
		Logger: logx.WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

func (l *GetUserLogic) GetUser(req *types.GetUserReq) (resp *types.GetUserResp, err error) {
	result, err := l.svcCtx.UserService.GetUser(l.ctx, &pb.GetUserReq{UserId: req.UserId})
	if err != nil {
		return nil, fmt.Errorf("获取用户远程RPC错误: %w", err)
	}
	// DB 1=公开 → true；2=私密 → false
	favoritesVisible := result.User.FavoritesVisibility == 1
	return &types.GetUserResp{
		Id:               result.User.Id,
		Username:         result.User.Username,
		Nickname:         result.User.Nickname,
		AvatarUrl:        result.User.AvatarUrl,
		Bio:              result.User.Bio,
		Level:            result.User.Level,
		FollowerCount:    result.User.FollowerCount,
		FollowingCount:   result.User.FollowingCount,
		PostCount:        result.User.PostCount,
		FavoritesVisible: favoritesVisible,
	}, nil
}
```

- [ ] **Step 2：删除已不再使用的 errx 引用**

（如果 imports 里还有 `"errx"`，去掉；gofmt/goimports 会自动处理）

- [ ] **Step 3：验证编译**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: 通过。

- [ ] **Step 4：提交**

```bash
git add app/gateway/internal/logic/user/get_user_logic.go
git commit -m "feat(gateway): GetUser 改为公开接口，加 FavoritesVisible"
```

---

## Task 15：Gateway GetUserPosts logic

**Files:**
- Modify: `app/gateway/internal/logic/user/get_user_posts_logic.go`

- [ ] **Step 1：完整替换 goctl 生成的空骨架**

```go
package user

import (
	"context"
	"esx/app/content/contentservice"
	"fmt"

	"gateway/internal/svc"
	"gateway/internal/types"

	"github.com/zeromicro/go-zero/core/logx"
)

type GetUserPostsLogic struct {
	logx.Logger
	ctx    context.Context
	svcCtx *svc.ServiceContext
}

func NewGetUserPostsLogic(ctx context.Context, svcCtx *svc.ServiceContext) *GetUserPostsLogic {
	return &GetUserPostsLogic{
		Logger: logx.WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// GetUserPosts 获取用户发布的帖子列表（公开接口）
func (l *GetUserPostsLogic) GetUserPosts(req *types.GetUserPostsReq) (*types.GetPostListResp, error) {
	result, err := l.svcCtx.ContentService.GetUserPosts(l.ctx, &contentservice.GetUserPostsReq{
		UserId:   req.UserId,
		Page:     req.Page,
		PageSize: req.PageSize,
		SortBy:   req.SortBy,
	})
	if err != nil {
		return nil, fmt.Errorf("获取用户帖子失败: %w", err)
	}

	list := make([]types.PostItem, 0, len(result.Posts))
	for _, post := range result.Posts {
		list = append(list, types.PostItem{
			Id:            post.Id,
			AuthorId:      post.AuthorId,
			Title:         post.Title,
			Content:       post.Content,
			Images:        post.Images,
			Tags:          post.Tags,
			ViewCount:     post.ViewCount,
			LikeCount:     post.LikeCount,
			CommentCount:  post.CommentCount,
			FavoriteCount: post.FavoriteCount,
			IsLiked:       false, // TODO: Interaction 服务实现后填充
			IsFavorited:   false, // TODO: Interaction 服务实现后填充
			CreatedAt:     post.CreatedAt,
		})
	}

	return &types.GetPostListResp{
		List:     list,
		Total:    result.Total,
		Page:     req.Page,
		PageSize: req.PageSize,
	}, nil
}
```

- [ ] **Step 2：验证编译**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: 通过。

- [ ] **Step 3：提交**

```bash
git add app/gateway/internal/logic/user/get_user_posts_logic.go
git commit -m "feat(gateway): 实现 GetUserPosts logic"
```

---

## Task 16：Gateway GetUserFavorites logic（权限 + stub 空列表）

**Files:**
- Modify: `app/gateway/internal/logic/user/get_user_favorites_logic.go`

- [ ] **Step 1：完整替换 goctl 生成骨架**

```go
package user

import (
	"context"
	"errx"
	"fmt"
	"user/pb/xiaobaihe/user/pb"

	"gateway/internal/svc"
	"gateway/internal/types"

	"github.com/zeromicro/go-zero/core/logx"
)

type GetUserFavoritesLogic struct {
	logx.Logger
	ctx    context.Context
	svcCtx *svc.ServiceContext
}

func NewGetUserFavoritesLogic(ctx context.Context, svcCtx *svc.ServiceContext) *GetUserFavoritesLogic {
	return &GetUserFavoritesLogic{
		Logger: logx.WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// GetUserFavorites 获取用户的收藏帖子列表
// 本次 Interaction 服务未实现：统一返回空列表。权限判断已落地。
func (l *GetUserFavoritesLogic) GetUserFavorites(req *types.GetUserFavoritesReq) (*types.GetPostListResp, error) {
	// 拿请求者（可能为 0，代表未登录）
	requesterId, _ := l.ctx.Value("userId").(int64)

	// 查目标用户以取得 favorites_visibility
	userResp, err := l.svcCtx.UserService.GetUser(l.ctx, &pb.GetUserReq{UserId: req.UserId})
	if err != nil {
		return nil, fmt.Errorf("查询目标用户失败: %w", err)
	}
	if userResp.User == nil {
		return nil, errx.NewWithCode(errx.UserNotFound)
	}

	// 权限判断：本人 or 公开
	isOwner := requesterId != 0 && requesterId == req.UserId
	isPublic := userResp.User.FavoritesVisibility == 1
	if !isOwner && !isPublic {
		return nil, errx.NewWithCode(errx.FavoritesPrivate)
	}

	// TODO: Interaction 服务实现后：
	//   1) 调 InteractionService.GetFavoriteList(req.UserId, page, pageSize) 获取 post_ids
	//   2) 调 ContentService.GetPostsByIds(post_ids) 获取帖子
	//   3) 调 BatchCheckLiked/BatchCheckFavorited 填充 isLiked/isFavorited
	return &types.GetPostListResp{
		List:     []types.PostItem{},
		Total:    0,
		Page:     req.Page,
		PageSize: req.PageSize,
	}, nil
}
```

- [ ] **Step 2：验证编译**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: 通过。

- [ ] **Step 3：提交**

```bash
git add app/gateway/internal/logic/user/get_user_favorites_logic.go
git commit -m "feat(gateway): 实现 GetUserFavorites（权限判断 + stub 空列表）"
```

---

## Task 17：Gateway UploadImage handler 手写 multipart 解析

**Files:**
- Modify: `app/gateway/internal/handler/image/upload_image_handler.go`

- [ ] **Step 1：完整替换 handler**

```go
// Code scaffolded by goctl. Safe to edit.
// goctl 1.10.1

package image

import (
	"errx"
	"net/http"

	"gateway/internal/logic/image"
	"gateway/internal/svc"

	"github.com/zeromicro/go-zero/rest/httpx"
)

const maxUploadSize = 10 << 20 // 10 MB

var allowedImageTypes = map[string]struct{}{
	"image/jpeg": {},
	"image/png":  {},
	"image/webp": {},
}

// 上传图片（multipart/form-data）
func UploadImageHandler(svcCtx *svc.ServiceContext) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		// 限制请求体大小
		r.Body = http.MaxBytesReader(w, r.Body, maxUploadSize)
		if err := r.ParseMultipartForm(maxUploadSize); err != nil {
			httpx.ErrorCtx(r.Context(), w, errx.NewWithCode(errx.FileTooLarge))
			return
		}
		file, header, err := r.FormFile("file")
		if err != nil {
			httpx.ErrorCtx(r.Context(), w, errx.NewWithCode(errx.ParamError))
			return
		}
		defer file.Close()

		ct := header.Header.Get("Content-Type")
		if _, ok := allowedImageTypes[ct]; !ok {
			httpx.ErrorCtx(r.Context(), w, errx.NewWithCode(errx.FileTypeNotAllowed))
			return
		}

		l := image.NewUploadImageLogic(r.Context(), svcCtx)
		resp, err := l.UploadImageMultipart(file, header)
		if err != nil {
			httpx.ErrorCtx(r.Context(), w, err)
			return
		}
		httpx.OkJsonCtx(r.Context(), w, resp)
	}
}
```

- [ ] **Step 2：验证编译**

（logic 里 `UploadImageMultipart` 方法在下一任务创建，这里会暂时编译错误 — 预期）

- [ ] **Step 3：提交**

```bash
git add app/gateway/internal/handler/image/upload_image_handler.go
git commit -m "feat(gateway): upload image handler 手写 multipart 解析与校验"
```

---

## Task 18：Gateway UploadImage logic 走 Media RPC streaming

**Files:**
- Modify: `app/gateway/internal/logic/image/upload_image_logic.go`

- [ ] **Step 1：完整替换 logic**

```go
// Code scaffolded by goctl. Safe to edit.
// goctl 1.10.1

package image

import (
	"context"
	"errx"
	mediapb "esx/app/media/pb/xiaobaihe/media/pb"
	"fmt"
	"io"
	"mime/multipart"

	"gateway/internal/svc"
	"gateway/internal/types"

	"github.com/zeromicro/go-zero/core/logx"
)

const chunkSize = 1 << 20 // 1 MB per chunk

type UploadImageLogic struct {
	logx.Logger
	ctx    context.Context
	svcCtx *svc.ServiceContext
}

func NewUploadImageLogic(ctx context.Context, svcCtx *svc.ServiceContext) *UploadImageLogic {
	return &UploadImageLogic{
		Logger: logx.WithContext(ctx),
		ctx:    ctx,
		svcCtx: svcCtx,
	}
}

// UploadImage 保留原签名以保证 types 匹配（返回未实现错误），
// 实际业务入口是 handler 调用的 UploadImageMultipart。
func (l *UploadImageLogic) UploadImage(req *types.UploadImageReq) (resp *types.UploadImageResp, err error) {
	return nil, errx.NewWithCode(errx.SystemError)
}

// UploadImageMultipart 从 handler 接收 multipart 文件，streaming 调 Media RPC。
func (l *UploadImageLogic) UploadImageMultipart(file multipart.File, header *multipart.FileHeader) (*types.UploadImageResp, error) {
	userId, _ := l.ctx.Value("userId").(int64)
	if userId == 0 {
		return nil, errx.NewWithCode(errx.LoginRequired)
	}

	stream, err := l.svcCtx.MediaService.UploadImage(l.ctx)
	if err != nil {
		return nil, fmt.Errorf("建立 media 流失败: %w", err)
	}

	// 首包：meta
	if err := stream.Send(&mediapb.UploadImageReq{
		Data: &mediapb.UploadImageReq_Meta{
			Meta: &mediapb.UploadMeta{
				UserId:   userId,
				FileName: header.Filename,
				Quality:  85,
			},
		},
	}); err != nil {
		return nil, fmt.Errorf("发送 meta 失败: %w", err)
	}

	// 分块发送
	buf := make([]byte, chunkSize)
	for {
		n, readErr := file.Read(buf)
		if n > 0 {
			chunk := make([]byte, n)
			copy(chunk, buf[:n])
			if err := stream.Send(&mediapb.UploadImageReq{
				Data: &mediapb.UploadImageReq_Chunk{Chunk: chunk},
			}); err != nil {
				return nil, fmt.Errorf("发送 chunk 失败: %w", err)
			}
		}
		if readErr == io.EOF {
			break
		}
		if readErr != nil {
			return nil, fmt.Errorf("读取文件失败: %w", readErr)
		}
	}

	mediaResp, err := stream.CloseAndRecv()
	if err != nil {
		return nil, fmt.Errorf("关闭流失败: %w", err)
	}
	if mediaResp == nil || mediaResp.Media == nil {
		return nil, errx.NewWithCode(errx.UploadFailed)
	}

	return &types.UploadImageResp{
		MediaId:      mediaResp.Media.Id,
		Url:          mediaResp.Media.Url,
		ThumbnailUrl: mediaResp.Media.ThumbnailUrl,
	}, nil
}
```

**注意**：`mediapb.UploadImageReq_Meta` / `mediapb.UploadImageReq_Chunk` 的具体类型名取决于 protoc 生成规则。如果实际生成的 oneof wrapper 名不同（如 `*UploadImageReq_Meta`、`*UploadImageReq_Chunk`），按生成代码实际类型调整。用 `grep` 验证：

```bash
grep "UploadImageReq_" app/media/pb/xiaobaihe/media/pb/media.pb.go | head -5
```

- [ ] **Step 2：验证编译**

```bash
cd app/gateway && go build ./... 2>&1 | tail
```
Expected: 通过。如果 oneof 类型名不匹配，按上面 grep 输出调整。

- [ ] **Step 3：提交**

```bash
git add app/gateway/internal/logic/image/upload_image_logic.go
git commit -m "feat(gateway): upload image 走 Media RPC streaming"
```

---

## Task 19：集成测试—用户帖子列表排序（Content 层）

**Files:**
- Modify: `app/content/internal/logic/integration_test.go` (or new file)

- [ ] **Step 1：在现有 `integration_test.go` 末尾或独立文件 `get_user_posts_integration_test.go` 中新增**

```go
func TestIntegration_GetUserPosts_SortBy(t *testing.T) {
	if testing.Short() {
		t.Skip("skip integration test in short mode")
	}
	// 依赖 testIntegrationSvcCtx（现有测试文件中应有 setup；如无，跳过）
	// ... 按现有 pattern 构造：插入 3 个帖子（不同 created_at、like_count），
	// 分别调用 SortBy=1 / SortBy=2 验证顺序。
}
```

由于测试依赖项目现有 integration harness，若此项目已有 `integration_test.go` 的 setup/teardown 与 truncate 函数，复用它们。如果没有，建立最小 setup 连接测试库并在 teardown 里清表。

- [ ] **Step 2：运行集成测试**

```bash
cd app/content && go test ./internal/logic/ -run TestIntegration_GetUserPosts_SortBy -race 2>&1 | tail
```
Expected: PASS。

- [ ] **Step 3：提交**

```bash
git add app/content/internal/logic/
git commit -m "test(content): 新增 GetUserPosts sortBy 集成测试"
```

---

## Task 20：Gateway GetUserFavorites 权限单元测试

**Files:**
- Create: `app/gateway/internal/logic/user/get_user_favorites_logic_test.go`

- [ ] **Step 1：建立 mock UserService（go-zero zrpc client 的 interface mock）**

由于 `svc.UserService` 是 goctl 生成的 interface，可以用 testify mock 实现。参考 content 层 mock 模式，在 gateway 测试中新建：

```go
package user

import (
	"context"
	"testing"

	"errx"
	userpb "user/pb/xiaobaihe/user/pb"

	"gateway/internal/svc"
	"gateway/internal/types"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/mock"
	"github.com/stretchr/testify/require"
	"google.golang.org/grpc"
)

type mockUserService struct {
	mock.Mock
}

// 仅 mock 本测试需要的 GetUser；其余方法留空（返回 zero-value）
func (m *mockUserService) GetUser(ctx context.Context, in *userpb.GetUserReq, opts ...grpc.CallOption) (*userpb.GetUserResp, error) {
	args := m.Called(ctx, in)
	if args.Get(0) == nil {
		return nil, args.Error(1)
	}
	return args.Get(0).(*userpb.GetUserResp), args.Error(1)
}

// TODO: 其他方法返回 zero-value 以满足 interface；
// 写一个通用 helper 或用 embed 让接口满足编译。
```

**关键**：`userservice.UserService` 是 goctl 生成的接口，可能有多个方法。需要 mock 所有方法（即使返回 nil）。实用做法——用 go mock 工具或手写一个 stub struct 内嵌接口并只覆盖 GetUser。

如果嫌麻烦，退而采用 **集成测试** 风格：把 `UserService` 字段设为 nil，仅跑权限逻辑——不现实，因为 logic 会调用 UserService。

**推荐简化路径**：在 logic 里对 `l.svcCtx.UserService` 做小抽象，提取一个 `userGetter` interface，测试时只 mock 这个小接口。

由于这会扩大改动范围，**本次单元测试暂用表驱动 + 手写 stub**，若太繁琐则跳过本任务，改为手动 Postman 验证。

- [ ] **Step 2：表驱动测试四组合**

```go
func TestGetUserFavoritesLogic_Permission(t *testing.T) {
	tests := []struct {
		name          string
		ctxUserId     int64
		targetUserId  int64
		targetVisible int32 // 1=公开 2=私密
		wantErrCode   int
	}{
		{"本人私密可访问", 100, 100, 2, 0},
		{"本人公开可访问", 100, 100, 1, 0},
		{"他人公开可访问", 100, 200, 1, 0},
		{"他人私密 403", 100, 200, 2, errx.FavoritesPrivate},
		{"未登录访问公开", 0, 200, 1, 0},
		{"未登录访问私密 403", 0, 200, 2, errx.FavoritesPrivate},
	}
	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			svc := &mockUserService{}
			svc.On("GetUser", mock.Anything, mock.Anything).Return(&userpb.GetUserResp{
				User: &userpb.UserInfo{Id: tt.targetUserId, FavoritesVisibility: tt.targetVisible},
			}, nil)

			ctx := context.Background()
			if tt.ctxUserId != 0 {
				ctx = context.WithValue(ctx, "userId", tt.ctxUserId)
			}
			svcCtx := &svc.ServiceContext{UserService: svc}
			l := NewGetUserFavoritesLogic(ctx, svcCtx)
			_, err := l.GetUserFavorites(&types.GetUserFavoritesReq{UserId: tt.targetUserId, Page: 1, PageSize: 10})
			if tt.wantErrCode == 0 {
				require.NoError(t, err)
			} else {
				require.Error(t, err)
				// 这里按 errx 实际 API 校验：
				assert.Contains(t, err.Error(), errx.GetMsg(tt.wantErrCode))
			}
		})
	}
}
```

**注意**：实际跑时若 `mockUserService` 不满足 `userservice.UserService` 完整接口，会编译失败。两种处理方案：

1. **简单方案**：为 mockUserService 添加其余方法的空实现
2. **复杂方案**：把 UserService 拆成小接口注入

如果因此陷入泥潭，**本任务允许跳过**，标记为"后续跟进"，改为在 Task 22 手动验证中覆盖权限矩阵。

- [ ] **Step 3：运行测试**

```bash
cd app/gateway && go test ./internal/logic/user/ -race 2>&1 | tail
```

- [ ] **Step 4：提交**

```bash
git add app/gateway/internal/logic/user/get_user_favorites_logic_test.go
git commit -m "test(gateway): GetUserFavorites 权限矩阵测试"
```

---

## Task 21：整体编译 + 测试

**Files:** —

- [ ] **Step 1：根目录整体编译**

```bash
cd "D:/Learning/projects/work projects/little-white-box" && go build ./... 2>&1 | tail
```
Expected: 全部通过。

- [ ] **Step 2：跑全部单测（非集成）**

```bash
cd "D:/Learning/projects/work projects/little-white-box" && go test ./... -short -race 2>&1 | tail -30
```
Expected: 通过，或明确标注跳过集成测试。

- [ ] **Step 3：跑 gosec 安全扫描（如已安装）**

```bash
gosec ./... 2>&1 | tail -20 || echo "gosec 未安装，跳过"
```

- [ ] **Step 4：若有未跟踪/失败，不进入下一步**

不要为了通过编译跳过任何实际错误；回到对应 task 修复。

---

## Task 22：手动端到端验证

**Files:** —

- [ ] **Step 1：启动所有服务**

依次启动（如使用 docker-compose 则一键）：

```bash
# 终端 1
cd app/user && go run user.go -f etc/user.yaml

# 终端 2
cd app/content && go run content.go -f etc/content.yaml

# 终端 3（Media 服务未实现 logic，上传请求会失败；此终端可不启）
# cd app/media && go run media.go -f etc/media.yaml

# 终端 4
cd app/gateway && go run gateway.go -f etc/gateway.yaml
```

- [ ] **Step 2：用 curl 验证接口 1（公开）**

```bash
curl "http://localhost:8888/api/v1/users/100/posts?page=1&pageSize=10&sortBy=1"
```
Expected: HTTP 200，`{code:0, data:{list:..., total:..., page:1, pageSize:10}}`。

- [ ] **Step 3：验证接口 2（公开用户）**

先把 userId=100 的 `favorites_visibility` 设为 1：

```sql
UPDATE user_profile SET favorites_visibility=1 WHERE id=100;
```

```bash
curl "http://localhost:8888/api/v1/users/100/favorites?page=1&pageSize=10"
```
Expected: HTTP 200，`data.list=[]`，`total=0`（因 Interaction 未实现）。

- [ ] **Step 4：验证接口 2（私密用户 → 403）**

```sql
UPDATE user_profile SET favorites_visibility=2 WHERE id=100;
```

未登录访问：
```bash
curl -i "http://localhost:8888/api/v1/users/100/favorites?page=1&pageSize=10"
```
Expected: body 包含 `"收藏列表已设为私密"`。

本人访问（带 Token）：
```bash
curl -i -H "Authorization: Bearer <token_for_user_100>" \
  "http://localhost:8888/api/v1/users/100/favorites?page=1&pageSize=10"
```
Expected: HTTP 200（本人可看自己的私密收藏）。

- [ ] **Step 5：验证接口 3（图片上传）**

未登录：
```bash
curl -i -X POST http://localhost:8888/api/v1/media/image -F "file=@test.jpg"
```
Expected: 401。

登录后：
```bash
curl -i -X POST http://localhost:8888/api/v1/media/image \
  -H "Authorization: Bearer <token>" \
  -F "file=@test.jpg"
```
Expected：
- 若 Media 服务未启：500（grpc client 连不上）— 符合预期，后续 Media 实现后恢复
- 若 Media 服务已启：200 + `{data:{url:..., mediaId:..., thumbnailUrl:...}}`

超大文件（12MB）：
```bash
curl -i -X POST http://localhost:8888/api/v1/media/image \
  -H "Authorization: Bearer <token>" \
  -F "file=@big-12mb.jpg"
```
Expected: 400 / `"文件过大"`。

非图片类型：
```bash
curl -i -X POST http://localhost:8888/api/v1/media/image \
  -H "Authorization: Bearer <token>" \
  -F "file=@somefile.pdf"
```
Expected: 400 / `"文件类型不支持"`。

- [ ] **Step 6：验证 GetUser 公开 + FavoritesVisible**

```bash
curl "http://localhost:8888/api/v1/user/100"
```
Expected: HTTP 200，body 含 `favoritesVisible: true/false`。

- [ ] **Step 7：交付勾选**

回到 `doc/2026-04-11-后端待补接口.md` 文末「交付清单」，勾选已完成项。

- [ ] **Step 8：提交勾选**

```bash
git add doc/2026-04-11-后端待补接口.md
git commit -m "docs: 勾选后端待补接口交付清单"
```

---

## 执行说明

- 提交粒度：每任务一次 commit，commit message 用 conventional commits 风格
- 遇到 oneof proto 类型名与 Task 18 示例不一致，按 `grep "UploadImageReq_" app/media/pb/...` 实际输出调整
- Task 20 单元测试如因 mock 繁琐卡住，降级为 Task 22 手动验证覆盖
- Interaction / Media 的实际 server logic 不在本计划范围；留 TODO 注释即可
