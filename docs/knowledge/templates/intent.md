# 意图页面模板

此文件只是人类使用的格式模板，不是正式意图。

```yaml
---
id: INT-example
layer: intent
title: Replace with a human-defined intent
status: draft
owner: human
upstream:
---
```

正文至少说明目标、成功标准、边界和非目标。人类确认后将状态改为 `approved`。
