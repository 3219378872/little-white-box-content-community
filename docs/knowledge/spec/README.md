# 规范层

本目录当前只有占位说明，不包含任何正式项目规范。

- 所有内容由人类开发者创建和维护，agent 永久只读。
- 规范描述可观察行为、约束、兼容边界和验收标准，不指定内部实现方案。
- 每份 `SPEC-*` 必须引用对应 `INT-*`；只有 `status: approved` 的规范可约束活跃设计。
- agent 的建议必须写入 `../proposals/`，不能在本目录起草或代为提升。

人类创建页面时使用 `../templates/spec.md`，并在本页维护索引。
