# 意图层

本目录当前只有占位说明，不包含任何正式项目意图。

- 所有内容由人类开发者创建和维护，agent 永久只读。
- 意图说明产品目标、成功标准、边界和非目标，不描述具体技术方案。
- 只有 `status: approved` 的 `INT-*` 页面才能约束规范和后续工作。
- agent 的建议必须写入 `../proposals/`，不能在本目录起草或代为提升。

人类创建页面时使用 `../templates/intent.md`，并在本页维护索引。
